<IfModule mod_rewrite.c>
  RewriteEngine On

  # Allow existing files and folders
  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]

  # Send all other routes to Next.js app
  RewriteRule ^ index.html [L]
</IfModule>